# Context

Building a single-page landing site for **Dr. Sara Shuttari**, Cybersecurity & Information Technology Professional.

**Read `PLAN.md` first** — it contains the full agreed spec: stack, palette, section-by-section breakdown, the interactive workshops panel, data model, and photo folder structure. All design decisions have already been made with the user; the "Decisions already made" section at the bottom lists them explicitly. Don't re-litigate those.

Source images in `assets/` have been processed into `public/logo.png` and `public/sara.png`.

Scaffolded and built — all sections, the workshops panel, and the lightbox are implemented and verified (build, lint, responsive, a11y). Real workshop photos still need to be dropped into `public/workshops/<slug>/` (currently all placeholder). See PLAN.md's Verification checklist for what's been confirmed.
